import { AppStub } from '../_stub/AppStub';

export default function DetourPage() {
  return (
    <AppStub
      app="detour"
      accent="det"
      title="a walk that never shows you the route"
      pitch="Give it a time budget, an endpoint and a mood. It walks you somewhere you have never been, one sentence at a time."
    />
  );
}
