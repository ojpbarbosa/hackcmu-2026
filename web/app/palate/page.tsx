import { AppStub } from '../_stub/AppStub';

export default function PalatePage() {
  return (
    <AppStub
      app="palate"
      accent="pal"
      title="any menu, ranked for your palate"
      pitch="Log what you love and your palate builds as a flavour profile. No dish is marked safe without a record."
    />
  );
}
