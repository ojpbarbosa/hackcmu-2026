import { AppStub } from '../_stub/AppStub';

export default function CastPage() {
  return (
    <AppStub
      app="cast"
      accent="cast"
      title="one prompt, every phone, same second"
      pitch="Answers stay locked until everyone has answered, then reveal together. A plan goes live only when enough people pull the line."
    />
  );
}
